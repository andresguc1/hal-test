PORT=2001
# OPENAI_API_KEY=
AUTH_ENABLED=false

# Optional: only needed to enable real authentication (cloud mode).
# When unset, the app runs in guest mode automatically.
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
# HALTEST_MASTER_ENCRYPTION_KEY=
